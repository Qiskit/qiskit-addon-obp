---
fixes:
  - |
    The :func:`~qiskit_addon_obp.utils.truncating.setup_budget` erroneously distributed the ``max_error_total``
    when ``num_slices`` was also set. This has been fixed now, such that the budget
    always gets distributed evenly, regardless of the value of ``p_norm``.
